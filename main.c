#include <stdio.h>

int main()
{
    int number = 0;
    int other = 27;
    float real = 4.5;

    int result = number + other;

    printf("Hello world %i %i %i %i\n", number, other, result, real);
}